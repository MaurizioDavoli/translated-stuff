CREATE TABLE IF NOT EXISTS people (
  product_id SMALLSERIAL,
  name varchar(250) NOT NULL,
  address varchar(500),
  PRIMARY KEY (product_id)
);

CREATE TABLE IF NOT EXISTS aaaa (
  provaaaaaa INT NOT NULL
);